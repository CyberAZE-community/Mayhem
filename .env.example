# Full websocket URL of the Havoc teamserver's 3rd-party service endpoint.
# Must match the Host/Port and Endpoint set under [Service] in your teamserver
# profile (.yaotl). Example: wss://127.0.0.1:40056/mayhem
service_endpoint=

# Password from the [Service] block of the same teamserver profile.
service_password=
